---
const year = new Date().getFullYear();
const columns = [
  {
    title: "Produit",
    links: [
      { label: "Fonctionnalités", href: "#features" },
      { label: "Aperçu", href: "#preview" },
      { label: "Inviter", href: "https://discord.com/oauth2/authorize" },
    ],
  },
  {
    title: "Confiance",
    links: [
      { label: "Sécurité", href: "#trust" },
      { label: "RGPD", href: "#trust" },
      { label: "Support", href: "https://discord.gg" },
    ],
  },
  {
    title: "Ressources",
    links: [
      { label: "GitHub", href: "https://github.com" },
      { label: "Statut", href: "#" },
      { label: "Blog", href: "#" },
    ],
  },
];
---

<footer class="footer">
  <div class="section-shell footer-shell">
    <div class="brand">
      <div class="mark">
        <span class="dot"></span>
        <span class="ring"></span>
      </div>
      <div>
        <p class="name">AlphaLLM</p>
        <p class="tag">Bot Discord IA</p>
      </div>
    </div>

    <div class="cols">
      {columns.map((col) => (
        <div class="col">
          <h4>{col.title}</h4>
          <ul>
            {col.links.map((link) => (
              <li><a href={link.href}>{link.label}</a></li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <div class="bottom">
      <span>© {year} AlphaLLM. Tous droits réservés.</span>
      <span class="made">Construit pour Discord.</span>
    </div>
  </div>
</footer>

<style>
  .footer {
    margin-top: 4rem;
    padding: 3rem 0 2rem;
    border-top: 1px solid var(--glass-border);
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.02), transparent);
  }

  .footer-shell {
    display: grid;
    gap: 2rem;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
  }

  .mark {
    position: relative;
    width: 42px;
    height: 42px;
    border-radius: 14px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    display: grid;
    place-items: center;
  }

  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--accent-iris), var(--accent-apricot));
  }

  .ring {
    position: absolute;
    inset: 6px;
    border: 1px dashed rgba(255, 255, 255, 0.2);
    border-radius: 12px;
  }

  .name {
    font-weight: 800;
    margin: 0;
  }

  .tag {
    margin: 0;
    color: var(--color-muted);
    font-size: 0.9rem;
  }

  .cols {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 1.5rem;
  }

  h4 {
    margin-bottom: 0.75rem;
    font-size: 0.95rem;
    letter-spacing: 0.02em;
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.4rem;
  }

  li a {
    color: var(--color-muted);
    transition: color 0.25s var(--ease);
  }

  li a:hover {
    color: var(--color-text);
  }

  .bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    color: var(--color-dim);
    font-size: 0.95rem;
  }

  .made {
    color: var(--accent-iris);
    font-weight: 700;
  }

  @media (max-width: 640px) {
    .bottom {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
