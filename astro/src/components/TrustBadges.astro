---
const badges = [
  { icon: "🔒", title: "RGPD", subtitle: "Données en UE" },
  { icon: "🧠", title: "Mémoire Longue", subtitle: "Threads persistants" },
  { icon: "👁️", title: "Vision", subtitle: "OCR + analyse" },
  { icon: "⚡", title: "Réponses rapides", subtitle: "<1s sur GPT-4o" },
  { icon: "🎨", title: "Images", subtitle: "HD cohérentes" },
  { icon: "🛡️", title: "Modération", subtitle: "Filtres + logs" },
];
---

<section id="trust" class="trust">
  <div class="marquee">
    <div class="track">
      {[...badges, ...badges].map((badge) => (
        <div class="badge">
          <span class="icon">{badge.icon}</span>
          <div class="text">
            <span class="title">{badge.title}</span>
            <span class="subtitle">{badge.subtitle}</span>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .trust {
    border-block: 1px solid var(--glass-border);
    background: linear-gradient(180deg, transparent, rgba(139, 92, 246, 0.03), transparent);
    overflow: hidden;
  }

  .marquee {
    display: flex;
    overflow: hidden;
  }

  .track {
    display: flex;
    gap: 2rem;
    padding: 1.25rem 2rem;
    animation: slide 28s linear infinite;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    padding: 0.7rem 1rem;
    border-radius: 12px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    white-space: nowrap;
    transition: transform 0.25s var(--ease), border-color 0.25s var(--ease);
  }

  .badge:hover {
    transform: translateY(-3px);
    border-color: rgba(139, 92, 246, 0.3);
  }

  .icon {
    font-size: 1.2rem;
  }

  .text {
    display: flex;
    flex-direction: column;
    gap: 0.05rem;
  }

  .title {
    font-weight: 700;
  }

  .subtitle {
    color: var(--color-dim);
    font-size: 0.85rem;
  }

  @keyframes slide {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  @media (max-width: 640px) {
    .track {
      gap: 1.25rem;
      padding-inline: 1.25rem;
    }
  }
</style>
