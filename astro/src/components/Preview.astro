---
const steps = [
  {
    title: "Invitez et c'est parti",
    desc: "Ajoutez AlphaLLM avec un clic, choisissez vos salons autorisés et activez la mémoire longue pour certains fils.",
  },
  {
    title: "Parlez naturellement",
    desc: "Utilisez /ask, /vision ou répondez simplement en thread : le bot comprend et suit le contexte.",
  },
  {
    title: "Générez en images",
    desc: "Décrivez votre visuel, sélectionnez le modèle (DALL·E, SDXL, Flux) et recevez plusieurs variantes.",
  },
];
---

<section id="preview" class="preview">
  <div class="section-shell">
    <div class="preview-grid">
      <div class="copy">
        <div class="pill">
          <span class="pulse"></span>
          Démo Discord
        </div>
        <h2>Un aperçu clair de la conversation</h2>
        <p>
          Pas de faux messages ici : c'est exactement ce que voit votre serveur. Capture, résumé, images et réponses
          rangées au bon endroit.
        </p>

        <div class="steps">
          {steps.map((step, i) => (
            <div class="step">
              <span class="id">0{i + 1}</span>
              <div>
                <h3>{step.title}</h3>
                <p>{step.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>

      <div class="frame">
        <div class="frame-inner">
          <div class="frame-head">
            <div class="lights">
              <span></span><span></span><span></span>
            </div>
            <span class="chip">Thread privé</span>
          </div>
          <div class="frame-body">
            <div class="image">
              <img src="/landing-screenshot.png" alt="Capture AlphaLLM" loading="lazy" />
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .preview {
    padding: 6rem 0;
  }

  .preview-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2.5rem;
    align-items: center;
  }

  .copy h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    letter-spacing: -0.02em;
    margin: 0.7rem 0 0.5rem;
  }

  .copy p {
    color: var(--color-muted);
  }

  .steps {
    display: grid;
    gap: 1rem;
    margin-top: 1.5rem;
  }

  .step {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 0.85rem;
    padding: 0.9rem 1rem;
    border-radius: 14px;
    border: 1px solid var(--glass-border);
    background: var(--glass);
  }

  .id {
    font-weight: 800;
    color: var(--accent-iris);
  }

  .frame {
    position: relative;
    width: 100%;
    background: linear-gradient(160deg, rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0.02));
    border-radius: 22px;
    border: 1px solid var(--glass-border);
    box-shadow: var(--shadow-lg);
    overflow: hidden;
  }

  .frame::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 20% 10%, rgba(139, 92, 246, 0.18), transparent 45%),
      radial-gradient(circle at 90% 40%, rgba(249, 115, 22, 0.18), transparent 55%);
    filter: blur(60px);
    opacity: 0.7;
  }

  .frame-inner {
    position: relative;
    z-index: 1;
  }

  .frame-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.9rem 1.2rem;
    border-bottom: 1px solid var(--glass-border);
    background: rgba(5, 5, 8, 0.7);
  }

  .lights {
    display: flex;
    gap: 0.35rem;
  }

  .lights span {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.35);
  }

  .chip {
    padding: 0.35rem 0.7rem;
    border-radius: 999px;
    background: rgba(6, 182, 212, 0.12);
    border: 1px solid rgba(6, 182, 212, 0.35);
    color: #9ce7f4;
    font-weight: 700;
    font-size: 0.82rem;
  }

  .frame-body {
    padding: 1.1rem;
    display: grid;
    gap: 0.75rem;
  }

  .image img {
    width: 100%;
    border-radius: 14px;
    border: 1px solid var(--glass-border);
  }

  @media (max-width: 768px) {
    .steps {
      grid-template-columns: 1fr;
    }

    .message {
      grid-template-columns: auto 1fr;
    }
  }
</style>
