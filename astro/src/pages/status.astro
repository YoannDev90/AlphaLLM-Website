---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
---

<Layout title="Statut du service · AlphaLLM" description="Disponibilité et incidents récents du bot AlphaLLM.">
  <Header />
  <main class="page">
    <section class="hero">
      <div class="section-shell">
        <div class="pill">
          <span class="dot"></span>
          Statut en direct
        </div>
        <h1>Statut du service</h1>
        <p>Disponibilité du bot et incidents récents. En cas de problème, vérifiez ci-dessous et suivez notre Discord.</p>
      </div>
    </section>

    <section class="content section-shell">
      <article class="card overall">
        <header>
          <div>
            <p class="label">État global</p>
            <h2>Tout fonctionne</h2>
          </div>
          <div class="badge badge-ok">Operational</div>
        </header>
        <p>Les services principaux (réponses IA, mémoire, vision, API) sont disponibles. Dernière mise à jour : maintenant.</p>
        <div class="meta">
          <div>
            <p class="label">Uptime 30j</p>
            <p class="strong">99.9%</p>
          </div>
          <div>
            <p class="label">Incidents ouverts</p>
            <p class="strong">0</p>
          </div>
          <div>
            <p class="label">Mises à jour prévues</p>
            <p class="strong">Aucune</p>
          </div>
        </div>
      </article>

      <article class="card timeline">
        <h3>Historique des incidents</h3>
        <div class="item">
          <div class="dot ok"></div>
          <div>
            <p class="label">Aucun incident récent</p>
            <p class="muted">Nous publierons ici les coupures ou dégradations de service.</p>
          </div>
          <span class="time">—</span>
        </div>
      </article>

      <article class="card channels">
        <h3>Suivre l'état</h3>
        <div class="channel-list">
          <a class="channel" href="/invite-link" aria-label="Rejoindre le Discord">
            <span>Discord</span>
            <span class="pill small">Live updates</span>
          </a>
          <a class="channel" href="https://github.com/yerminator" target="_blank" rel="noreferrer">
            <span>GitHub</span>
            <span class="pill small">Changelog</span>
          </a>
          <a class="channel" href="mailto:yoanndev@outlook.fr">
            <span>Email</span>
            <span class="pill small">Support</span>
          </a>
        </div>
      </article>
    </section>
  </main>
  <Footer />
</Layout>

<style>
  .page {
    padding-top: 5rem;
  }

  .hero {
    padding: 4rem 0 2rem;
  }

  h1 {
    font-size: clamp(2.4rem, 5vw, 3.2rem);
    letter-spacing: -0.02em;
    margin: 0.5rem 0 0;
  }

  h2 {
    margin: 0;
  }

  h3 {
    margin: 0 0 0.5rem;
  }

  p {
    color: var(--color-muted);
  }

  .content {
    display: grid;
    gap: 1rem;
    padding-bottom: 3rem;
  }

  .card {
    padding: 1.3rem 1.4rem;
    border-radius: 16px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    display: grid;
    gap: 0.75rem;
  }

  .overall header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
  }

  .label {
    font-size: 0.9rem;
    color: var(--color-muted);
    margin: 0;
  }

  .strong {
    margin: 0;
    font-weight: 600;
    color: var(--color-primary);
  }

  .meta {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 0.75rem;
  }

  .badge {
    padding: 0.35rem 0.8rem;
    border-radius: 999px;
    font-size: 0.9rem;
    border: 1px solid var(--glass-border);
    background: rgba(74, 222, 128, 0.1);
    color: #4ade80;
  }

  .timeline .item {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 0.8rem;
  }

  .timeline .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--glass-border);
  }

  .timeline .dot.ok {
    background: #4ade80;
    box-shadow: 0 0 12px rgba(74, 222, 128, 0.4);
  }

  .muted {
    color: var(--color-muted);
    margin: 0;
  }

  .time {
    color: var(--color-muted);
    font-size: 0.9rem;
  }

  .channels .channel-list {
    display: grid;
    gap: 0.6rem;
  }

  .channel {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.9rem 1rem;
    border-radius: 12px;
    border: 1px solid var(--glass-border);
    background: rgba(255, 255, 255, 0.02);
    color: var(--color-foreground);
    text-decoration: none;
    transition: border-color 0.2s ease, transform 0.2s ease;
  }

  .channel:hover {
    border-color: var(--accent-iris);
    transform: translateY(-1px);
  }

  .pill.small {
    padding: 0.25rem 0.55rem;
    font-size: 0.85rem;
  }

  .dot {
    width: 8px;
    height: 8px;
    background: var(--accent-iris);
    border-radius: 50%;
    box-shadow: 0 0 12px rgba(139, 92, 246, 0.5);
  }
</style>
